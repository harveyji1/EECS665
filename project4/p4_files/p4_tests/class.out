student{student} : class {
    studentID{int} : int;
};
lame{lame} : class {
    lameness{int} : int;
};
main{() -> void} : () void  {
    harvey{myID->getSymbol()->getType()} : student{student};
    loser{myID->getSymbol()->getType()} : lame{lame};
}
